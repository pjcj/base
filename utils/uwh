#!/usr/bin/zsh

command which $1
echo
command whence -afps $1
echo
echo PATH is $PATH
perl -e '
for my $file (@ARGV) {
    for my $dir (split(/:/, $ENV{PATH})) {
        if (-f (my $path = "$dir/$file") && -x _) {
            system "ls", "-ld", $path;
        }
    }
}
' $1
