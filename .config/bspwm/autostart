#!/bin/sh

PATH=/home/pjcj/g/sw/bin:$PATH

xrdb -merge .Xdefaults
xsetroot -bg black
numlockx on
(
    sleep 1
    gnome-panel &
    clipit -d &
    st &
    # nm-applet &
    if [ "$(hostname)" = "pj" ]; then
        thunderbird &
        firefox &
        st irssi &
    fi
) &
